# ZT CRM Mobile Generator V5

在 Figma+EX 中打开任意 Design 文件，然后：

1. 进入 `插件 -> 开发 -> 从清单导入插件`。
2. 选择本目录中的 `manifest.json`。
3. 运行 `ZT CRM Mobile Generator V5`。

插件会创建一个名为 `ZT CRM Mobile V5` 的页面，其中包含 7 个 390 x 844 的手机端高保真画板：工作台、客户列表、客户详情、快速记录、记录确认、沟通记录列表、沟通记录详情。首页只聚合事实底座产生的待处理状态，不创建任务系统。
